::: tip Не забудьте:
По умолчанию установка сторонних тем и сценариев может быть отключена. Как включить установку сторонних расширений, описано в [данной статье](/workarrounds/enable-downloading-of-third-party-themes/).

Если не находится в поиске, вы можете найти его на странице <a target="_blank" :href="'https://store.kde.org/p/' + $frontmatter?.aggregation?.extension?.id">{{ $frontmatter?.appstream?.name }}</a> в KDE Store и проверьте доступность.
:::
